/**
 * Screen reader-only indicator for external links that open in new tabs.
 * Provides accessibility information to assistive technology users.
 */
export default function ExternalLinkIndicator() {
    return <span className="sr-only"> (opens in new tab)</span>;
}
